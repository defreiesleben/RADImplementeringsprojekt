using System;
using System.Numerics;
using System.Collections;
using System.Collections.Generic;
using System.Diagnostics;

namespace RADImplementationProject
{
    public class Program
    {
        public static void Main(string[] args)
        {
            //for (int i = 0; i < 64; i++)
            //    Console.WriteLine(i + ": " + twothpower(i));

            //for (int i = 1; i < 16; i++)
            //    Console.WriteLine("200 mod {0,5}: {1,3} =?= {2,3}"
            //        , HashFunctions.twothpower(i) - 1
            //        , HashFunctions.hackModulo(i, 200)
            //        , HashFunctions.normalModulo(i, 200));


            // l can be in range [0, 127]
            //int l = 7;
            //
            //for (ulong x = 0; x < 6; x++)
            //{
            //    BigInteger actual = HashFunction.MultiplyModPrime(x, l);
            //    BigInteger expected = HashFunction.MultiplyModPrimeReal(x, l);
            //    Console.WriteLine("x={0,1}, equal?: {1}\n\t{2}\n\t{3}"
            //        , x
            //        , actual == expected
            //        , actual
            //        , expected);
            //}
        }

    }

}